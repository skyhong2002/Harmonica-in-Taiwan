# The app serves both multilingual pages and the compatible legacy API/feed URLs.
# Run serve.py on 127.0.0.1:8330 with HARMONICA_PUBLIC_ORIGIN set below.
harmonica.observe.tw {
	encode zstd gzip
	reverse_proxy 127.0.0.1:8330
}
