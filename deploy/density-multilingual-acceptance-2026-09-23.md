# 活動、樂譜密度與多語名錄 — 2026-09-23

本次依最新使用者要求修改 `/events/`、`/scores/`、`/scores/sources/` 及 `/source/`，保留原版米色／綠色、頂部導覽及首頁限動→Google Calendar→三欄貼文。

## 問題與實作

- [#21 活動缺少原文與媒體](https://github.com/skyhong2002/Harmonica-in-Taiwan/issues/21)：桌面緊密雙欄、手機單欄。只有實際貼文網址／ID關聯才呈現原文、作者、追蹤與原始來源；圖片優先已有本機快取，影片使用原生控制。長原文可展開，海報完整顯示。保留當地時區、全天 exclusive end 與未公告結束時間。
- [#22 樂譜密度不足](https://github.com/skyhong2002/Harmonica-in-Taiwan/issues/22)：桌面使用全寬四欄表格，曲名與作者、年度／編制／組別、出版來源、佐證與操作對齊。手機改成可閱讀分區；出版／集合桌面雙欄。所有篩選、排序、分頁、原始備註與回報保留。
- [#23 名錄缺少多語名稱](https://github.com/skyhong2002/Harmonica-in-Taiwan/issues/23)：顯示目前介面語言、中文、英文及收錄原名，同文合併語言標籤。類型亦並列多語與原始類型，完整名稱不截斷，手機保留。不同語言名稱與別名皆可搜尋。

此次快照322筆來源均有四語參考名稱；五個批次經逐筆對照收錄名稱審閱，保留專名、縮寫和可識別的原始稱呼。

## 名稱資料契約

`data/sources/source-name-translations.json` 是公開參考譯名，不代表官方名稱。以既有來源 `id` 關聯；`sourceName`、`sourceNameEn` 必須與目前快照一致才採用，避免來源身份更新後沿用錯誤譯名。

`names.original` 保留收錄名稱，不會將中文編輯名稱冒稱為官方日文／韓文原名；`namesMeta` 標示 reference。原有 `name`、`nameEn`、連結、slug 與原始貼文維持。詞彙與機構描述提供參考翻譯，沒有可靠當地譯名的人名保留可識別專名。

翻譯使用既有本機 Codex 隔離推論並逐批審閱，共用既有每小時上限；沒有訪客即時推論、付費 API fallback 或提高 Apify 預算。新來源缺譯名或名稱改變時，先回退收錄名稱；維護者審閱並更新參考譯名資料後才顯示新譯名。

## 實際資料限制

樂譜是曲目與出版線索索引，沒有假造下載、譜面或封面。驗收快照39筆出版／集合的佐證網址與現有貼文沒有精確交集，原始資料也未提供圖片，因此不能聲稱已取得真實樂譜公告海報。介面及後端已支援未來有明確佐證的原文與多媒體；fixture 僅用於隔離測試，未寫入正式資料。

IG 遠端影片可能過期。驗收發現一支既有影片 CDN 已失效，頁面已實際驗證保留快取海報並改顯示四語原始影片入口，不留下失效播放控制。可用的影片仍使用原生控制。

## 驗證紀錄

- 完整252項Python、99項前端測試通過；四語正式API譯名搜尋及322筆原名保留檢查通過。

- 本機與正式 HTTPS 樂譜各48組瀏覽器檢查：1440／768／390、四語、明暗、曲目／集合；無橫向溢出，分頁、年度、搜尋、history、鍵盤備註與情境回報通過。
- 公開 HTTPS 活動四語與桌面／手機檢查：國家／期間／搜尋、history、原文展開、隔離分頁；實際捲動載入圖片與影片，無 JS error。
- 正式HTTPS名錄12組真實資料檢查：1440／820／390×四語，名稱、類型、跨語譯名搜尋、排序、追蹤及詳頁通過，無JS錯誤或橫向溢出；參考譯名不冒稱官方原名。
- 輸出、來源覆蓋、舊網址及 sitemap 驗證通過；sitemap 驗收快照401個網址，零錯誤。資料量由快照變動，不寫死在 UI。

瀏覽器截圖與檢查結果存於本機 `/tmp/harmonica-density/`、`/tmp/events-final-desktop-loaded.png`、`/tmp/events-final-mobile-loaded.png`。既有 web LaunchAgent 重啟載入 Python 更新；沒有修改 Caddy、其他網站或發佈 Pages。未修改／提交無關 `deploy/instagram-monitoring.md`。

## 後續：找譜頁的理解與操作（#24）

使用者指出 `/scores/sources/` 看不懂，後續將頁名及四語導航改為「找譜與購譜」。此節取代前述頁面以提供者為主標、混合排列的設計。

- 依原始 `format` 區分「譜集與教材」「販售公告」「曲庫與洽詢」，分類及計數依資料計算。驗收快照為23／4／12筆，共39筆資訊，不代表39家出版者。
- 每筆以書名／資訊名稱當主標，提供者、編制、取得方式分列；移除重複書名與收合狀態的長篇索引說明。
- 主要按鈕依實際URL區分商品頁、商店分類、商店、公告、粉專、曲庫及作品介紹。只有粉專網址的公告不冒稱可直達公告；商品分類不冒稱具體商品頁。
- 原始摘要、作者、價格、狀態與收錄日期保留於可展開紀錄。價格與狀態明確標示為收錄當時資訊，未宣稱目前庫存。
- 預設不展示全部未知的國家篩選；有已知地區或既有篩選條件時仍保留，未將未知地區猜為臺灣。
- 新分類篩選納入URL狀態、重新整理與路由返回；四語SSR標題與內容同步更新。

公開HTTPS 24組尺寸／語言／明暗檢查通過（1440、820、390 × 四語 × 明暗），另驗證分類、24→39分頁、搜尋、重新整理、路由history、鍵盤展開紀錄與重設。獨立代理實際查看三個分類，未發現阻擋理解或誤標操作。完整253 Python與100前端測試通過。證據保存於 `state/ui-acceptance-2026-09-23/score-guide/`。

## 後續：真實封面與公告圖片（#25）

使用者要求圖片後，由三個代理分別核對原有商品頁、分類頁及社群公告，已補齊22本譜集／教材封面，以及1張HAPA原始販售公告圖片。此進度取代前述「資料尚無圖片」的歷史限制。

- 商品封面逐一比對書名、版本、商品標題與原圖；Grade1／2／3、佐藤第1／2／3集及上下冊分開，未使用商家Logo或曲譜內頁。
- `data/sources/score-source-media.json` 保存收錄名稱、匹配標題、圖片URL、原始商品／公告頁及核對日期。採用前比對既有ID、書名及提供者，避免錯配到後續變更的書籍。
- 預覽快取於 `site/assets/feed-images/score-*.webp`，沿用既有WebP轉換及縮圖工具，合計約1MB；快取不進Git，來源與圖片manifest進Git。`scripts/cache_score_source_images.py` 僅由維護者明確執行，不會因訪客瀏覽下載圖片。新副本缺快取時沿用已記錄的原始圖片URL，離線建置不需抓圖。
- 封面旁保留書名、提供者與取得方式；封面連結回確實匹配的原頁，新確認的商品頁亦供主要操作使用。無圖片項目不放假封面或占位框。
- 丸玩琴既有公告照片實際為無關街頭合照：公開catalog保留既有ID／原題，排除該錯誤照片連結、轉至曲庫與洽詢並四語標示待確認，導回粉專。修正證據仍記錄於manifest。
- 十年日記尚無可確認封面；Judy只有粉專頭像；The Duet圖片為整頁曲譜，未當封面轉載。未登入或繞過存取限制，也未啟動Apify／Codex或提高預算。

公開HTTPS24組尺寸／四語／明暗檢查通過：23張圖片全部成功載入，無橫向溢出或JS錯誤，錯誤照片連結已移除、待確認公告歸類正確。完整254 Python及101前端測試通過，公開輸出驗證通過。截圖與驗收資料保存於 `state/ui-acceptance-2026-09-23/score-covers/`。


## 後續：短日期與活動／名錄頭像（#26）

- 貼文、限動、觀測與更新時間改用當年短日期加24小時制，例如 `Sep 22 · 18:00`；非當年保留年份，`time` 的 datetime、title與aria-label保留完整日期與時區。四語日期仍依介面語言顯示，貼文時間依裝置時區顯示。
- 活動使用自己的時區，同日共用日期（`Sep 25 · 20:30–21:30`），時區名稱保留在卡片；跨日／跨年保留兩端日期，DST重複時刻加上不同offset，未知結束時間不顯示推估終點。全天仍依civil date和exclusive end顯示，跨年判斷使用當地年份。
- 活動作者移到卡片頂端，桌面頭像56px、手機48px，名稱與平台／日期分行。相關原文、海報與額外來源作者保留，避免同一作者在海報下重複。
- 名錄頭像桌面72px、平板64px、手機60px，重分配名称欄與手機國家、類型及來源連結位置；完整多語名稱和歷史日期年份不截斷。

106項前端測試通過，含新增的跨年、DST、四語、civil date與完整時間提示測試。公開HTTPS驗收：名錄12組尺寸／四語互動測試及9個邊界寬度；活動四語×桌面／手機40項檢查；另外18組三頁×三尺寸×台北／洛杉磯瀏覽器時區檢查，皆無JS錯誤與橫向溢出，活動時刻一致，原文、追蹤、排序與詳頁正常。包含明暗模式與1440×900、820×900、390×844截圖。證據保存於 `state/ui-acceptance-2026-09-23/avatar-time/`。本輪僅前端及測試／紀錄變更，不需重啟Python服務。


## 後續：首頁限動持續同步（#27）

使用者回報限動仍未出現。唯讀對照竹梅並查核口琴正式API、新瀏覽器及本機快取，確認當時已有2則竹韻有效限動且圖片可載入；找到首頁只初次載入catalog、長開頁面只刪除過期項目卻不取得新內容的缺口。這能解釋舊頁停留空白，未宣稱已重現使用者原瀏覽器的確切狀態。

- 可見首頁每60秒讀取公開catalog；回前景、SPA與history返回首頁也同步。四語「更新限動」提供即時手動更新、成功／失敗提示與15秒逾時重試。
- 只增刪限動卡片，保留仍有效的媒體節點、播放時間、焦點、橫向捲動、搜尋草稿、日曆iframe與河道；離頁取消請求與監聽，逾時或舊回應不覆蓋新版。失敗保留原卡片，真實expiry仍生效。
- 「最近收錄」改讀有效限動observedAt，移除以catalog重建時間冒充限動更新時間的呈現。最後一筆過期後也清空時間。
- 竹梅fetch_stories.py以48h展示窗口輸出，而口琴維持provider原始到期時間。口琴已具備與竹梅相同目的的本機媒體快取，未共用其session或修改竹梅服務。
- 抓取覆盖仍受現有預算限制：本輪公開容量快照179來源、估計每天5來源，並非每日全量覆蓋承諾。未啟動新的Apify actor，未提高US$4站方月上限。頁面讀取頻率與外部抓取频率是不同事項。

114項前端測試全過，涵蓋保留影片播放、請求去重／取消／逾時／舊回應、有效期與四語狀態。公開HTTPS使用隔離瀏覽器mock驗收12組（1440／820／390×四語、含明暗）：空白→有效限動、503保留資料→重試恢復、圖片載入、無溢出／JS錯誤。另以browser clock驗證60秒自動更新、hidden暫停／回前景更新、SPA及history返回，保持輸入焦點與草稿、iframe和河道節點。最後不加mock重新驗收正式首頁：兩張真實圖片載入、最近收錄時間正確、手動刷新成功。證據在 `state/ui-acceptance-2026-09-23/story-refresh/`。


## 後續：依竹梅方式修正限動抓取與立即發布（#28）

使用者澄清要處理抓取方式。核對竹梅實際launchd／run_pipeline，確認兩站使用同一Apify Stories actor，未改用竹梅舊Instaloader或共用其帳號。修正口琴的活躍來源排序、同帳號金額／結果slot配對、擴充池的分批配速，維持原子預留與既有費用限制。另將pipeline拆為story→立即發布→profile→其他平台；新的publish_story_cache helper以原生鎖、定向無LLM watchdog及離線RSS／JSON完成發布，不再等完整抓取流水線。

實際驗收：臺北2026-09-23 10:07:50執行一次5帳號、最多5結果、最高US$0.0275的受限抓取；10:08:24已完成發布。返回3則，竹韻2則仍有效，新增Aiden Soon 1則。正式API與真瀏覽器確認3張圖皆成功載入、原始連結與expiry正確，無JS錯誤／溢出。不是重跑完整pipeline，沒有Calendar寫入、額外Codex推論或調高費用。初次run費用可能延後更新，驗收僅主張授權cap，不以初報0元當免費。

269項Python測試全通過，包含同帳號配對、不重複計算別名、縮批仍能reserve、unknown／denied完整預留、活躍來源與探索、多批配速與中止、seen補漏／損壞停止、無變動零重建，以及後續profile／YouTube失败前已發布。公開輸出、526來源／322名錄覆蓋、舊址與401條sitemap驗證皆通過。Web服務已按既有launchd重啟，證據在 `state/ui-acceptance-2026-09-23/story-collector/`。

已知限制：仍按獨立pool容量輪詢，無法保證所有179帳號每日全覆蓋；多帳號被provider明確拒絕時的帳號別退避／換池尚未加入，目前整輪停止並保留預留。未移植竹梅的48h展示期限、學校身份或憑證。

## 後續：介面語言與資訊精簡（#29）

使用者要求減少中英混列和不必要資訊，取代先前所有譯名同時展示的要求。名錄／來源詳頁預設只保留介面語言名稱及不同的收錄原名、單一介面語言分類；四語參考譯名、原始分類及翻譯說明放入「其他名稱與分類」。跨語搜尋資料保持完整，原文、頭像與原始來源連結不變。既有本地化主字標與英文副標保留。

- 動態與活動作者採用介面語言名稱，移除重複的原文宣言、裝置時區與處理方式長說明；網站快照仍明確區別觀測與發布，活動當地時區與未知結束時間仍可見。
- 網站、人工收錄、Instagram限動平台標籤隨語言顯示；更多選單的無障礙名稱不再堆疊四種語言，語言選擇仍完整。
- 樂譜已知編制、組別與取得方式翻譯成介面語言，原始資料、篩選值、曲名與作者未改寫；不明值保持原文。長篇索引與學年度說明移至可展開區，收錄／取得方式及來源連結保留。
- 活動重複指向同一URL的原始來源操作合併，圖片、影片與相關原始貼文保留。

117項前端測試通過；正式HTTPS瀏覽器60組檢查（5頁×四語×1440／820／390，含韓文暗色）均無頁面錯誤或橫向溢出，驗證名錄名稱展開／收合、追蹤、平台本地化與活動時區。兩個平行代理另完成來源12組及動態／活動／樂譜32組公開瀏覽器檢查，含跨語搜尋、鍵盤展開與組別篩選。證據保存於 `state/ui-acceptance-2026-09-23/quiet-ui/`。本輪只有靜態前端、測試與文件變更，沿用既有服務，不啟動抓取。
